{% extends 'base.html.twig' %}


{% block body %}



<div class="container">
    <div class="row">
    
        {% for pr in produits %}

            <div class="col-12  text-center ">
                <div class="m-1 vignette">
                    <a href="/prioduit/{{ pr.id }}">
                        {{ pr.nom }}
                    </a>
                </div>
            </div>

        {% endfor %}

    </div>
</div>
{% endblock %}
